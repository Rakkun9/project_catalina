# Supabase — Project Settings → API
# Son las dos únicas claves que necesita la app. La service-role key ya no se
# usa: el panel escribe con la sesión del usuario y RLS valida cada operación.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
